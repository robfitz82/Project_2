<% @photos.each_with_index do |p, index| %>	
	<% if index % 3 == 0 %>
	<div class="row">
		<div class="col-sm-4">
      <%= image_tag("#{p.url}-/resize/200x/") %>
   </div>
	<% else %>
	<div class="col-sm-4">
      <%= image_tag("#{p.url}-/resize/200x/") %>
   </div>
	<% end %> 
<% end %>